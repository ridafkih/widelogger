export default function ProvidersSettingsPage() {
  return (
    <div className="h-full flex items-center justify-center p-3">
      <span className="text-xs text-text-muted">Select a provider to configure</span>
    </div>
  );
}
